spring.application.name=dicovery-server
server.port=7000

# Disable self-registration and fetching
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Explicitly override the peer replication URL to match your port
eureka.client.service-url.defaultZone=http://localhost:7000/eureka/